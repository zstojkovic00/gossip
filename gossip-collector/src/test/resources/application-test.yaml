spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      properties:
        schema.registry.url: http://localhost:8083/apis/ccompat/v7
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password
